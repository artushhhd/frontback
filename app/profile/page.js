import ProfileBody from './profile';
import DeshboardContent from '../deshboard';
import './profile.css';

export const metadata = { title: 'profile' };

export default function Page() {
  return <ProfileBody />;
  <DeshboardContent />;
}